# 邻传 0.2.0 测试报告

日期：2026-09-23。环境：本机 Apple Silicon Mac。未安装应用；实际应用测试使用分发 ZIP 的临时解压副本。

## 结果

- 默认自动测试：16 项通过，0 项失败，运行约 0.45 秒（不含编译）。另外 2 个人工配合用例默认跳过。
- 覆盖中文文件名、空文件、32 MiB 文件完整性、重名不覆盖、错误密钥、暂停接收、接受与拒绝、等待确认时取消、接收中取消、传输中断清理、校验失败清理、损坏密文、超长帧及载荷、路径穿越与 Windows 保留名、地址与密钥校验、多文件队列，以及三个中文页面的无窗口渲染。
- 真实 Mac 分发包启动成功。通过接收确认弹窗收到中文文件，内容校验一致；收起后新请求唤起窗口，再次接收通过；使用原生文件选择器选文件并从界面发送到本机接收端，内容一致；点击退出后进程与监听端口均已关闭。
- 人工配合测试共执行 3 次：接收、后台接收、发送，全部通过。UDP 发现应答在本机回环地址验证通过。
- Mac / Windows ZIP 完整性检查通过；Windows EXE 和随包 libunwind.dll 为 x64 PE；分发文件 SHA-256 与清单一致。使用说明已更新，重新打包前后应用可执行文件哈希相同。

## 未覆盖

Windows 和 Intel Mac 实际启动及操作、两台电脑间的 Wi-Fi 发现与互传、防火墙和不同路由器环境、长时间压力和吞吐量测试、菜单栏菜单恢复入口、磁盘写满、系统睡眠恢复。当前结果不能作为真实 Wi-Fi 速度或 Windows 兼容性的证明。没有进行安全审计。

## 清理

测试应用已退出。已清理以下可重新生成的内容，删除前目录大小合计约 2.7 GiB（实际磁盘可用空间变化可能受 APFS 影响）：

- 项目 `.test-work/`：样例文件、接收文件、原始日志、测试密钥配置副本及解压的应用。
- 项目 `target/`：编译与测试缓存。
- 项目 `.build-tools/llvm-mingw.tar.xz`：工具链已解压，删除冗余下载压缩包。
- 本次创建的 `~/Library/Application Support/app.LanTransfer.LanTransfer/` 及应用偏好设置 `~/Library/Preferences/app.lantransfer.desktop.plist`。

保留源码、测试用例、正式分发文件和可复用的 LLVM-MinGW 工具链；未清理系统或其他项目缓存。重新运行测试或构建会重新生成 target 缓存。测试用法见 `tests/README.md`。
